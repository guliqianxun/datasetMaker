# Label Studio ML Backend – Python dependencies
# Pin minor versions for reproducibility; allow patch updates.

# Web framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0

# HTTP client (async + sync)
httpx>=0.27,<1.0

# AWS / MinIO S3 client
boto3>=1.34,<2.0

# Image processing
Pillow>=10.3,<12.0

# Configuration (pydantic-settings reads .env automatically)
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0

# ------------------------------------------------------------------ Dev / test
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
pytest-mock>=3.14,<4.0
